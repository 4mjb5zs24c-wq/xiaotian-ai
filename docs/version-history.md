# 小天智能助手 — 版本记录

> 本文档记录小天智能助手各模块的版本演进，每个模块独立版本号。
> 最新稳定版本为当前基线，后续新增规则归入下一版本。

---

## 版本概览

| 模块 | 当前版本 | 状态 | 基线文档 |
|------|:--:|:--:|------|
| 词汇洞察与复习方案 | v1.0 | 稳定基线 | [`v1.0/04_词汇洞察与复习方案规则口径说明.md`](./v1.0/04_词汇洞察与复习方案规则口径说明.md) |
| 首页智能搜索 | v1.1 | 已完成 | [`xiaotian-smart-search-v1.1-rules.md`](./xiaotian-smart-search-v1.1-rules.md) |
| AI 工作流（Workflow） | v1.0 | 稳定基线 | [`v1.0/03_技术实现说明_前端后端与规则逻辑.md`](./v1.0/03_技术实现说明_前端后端与规则逻辑.md) §6 |

---

## 首页智能搜索 — 版本记录

### v1.0（当前稳定基线）

- **基准日期**：2026-06-16
- **文档**：[`v1.0/06_AI搜索与智能助手资源匹配规则说明.md`](./v1.0/06_AI搜索与智能助手资源匹配规则说明.md)
- **代码位置**：
  - 搜索引擎：`src/ai/search-new/searchEngine.ts`
  - 类型定义：`src/ai/search-new/types.ts`
  - 场景工厂：`src/ai/search-new/mock/scenarios/`
  - UI 组件：`src/ai/components/search-new/`（15 个组件）
  - 页面入口：`src/pages/SearchPage.tsx`
  - 路由：`/ai-search`（App.tsx 中定义）

#### v1.0 主要能力

**1. 双态布局**
- 首页态：上下文条 + 搜索卡片（"小天智能助手"）+ 4 个快捷入口 + 近期使用标签
- 搜索结果态：上下文条 + 精简搜索栏 + 意图说明条 + 筛选标签 + 结果分组列表
- 状态切换：执行搜索 → 结果态；清空搜索词 → 首页态

**2. 搜索入口**
- 首页搜索框（首页右侧「小天AI智能助手」卡片）
- AI 搜索页（`/ai-search` 路由）
- 首页「小天AI」按钮（顶部栏右侧）
- 4 个快捷入口点击（当前单元资源/同步词汇/听力练习/快速制卡）
- 近期使用标签点击
- URL 参数自动搜索（`?query=xxx&autoRun=1`）

**3. 意图识别与路由（8 级优先级）**

| 优先级 | 场景 | 说明 |
|:--:|------|------|
| 1 | 功能入口关键词 | 答题卡/词表/听写/批改/导入/讲词/自定义 |
| 2 | 试卷名称精确搜索 | 省份+年份+考试类型解析，精确/相近两级匹配 |
| 3 | 同步词汇搜索 | 触发词：同步词汇/词汇表/单词表/课标词汇 |
| 4 | 同步课文搜索 | 触发词：同步课文/课文内容/课文原文/课文跟读 |
| 5 | 资源类型搜索（12 子优先级） | 5a 写作→5b 词汇→5c 语法→5d 阅读→5e 听力模拟→5f 听力→5g 听说→5h 同步练习→5i 真题→5j 专项→5k 模拟→5l 配音 |
| 6 | Unit N + 资源类型组合 | 识别 Unit 编号+资源类型关键词 |
| 7 | 模糊/默认/对话式查询 | "有没有"/"帮我找"/中文短词 2-4 字 |
| 8 | 完全无法识别 | 返回 3 个快捷入口建议 |

**4. 上下文感知**
- 自动感知教师当前教材版本、单元、年级、班级
- 搜索结果优先匹配当前教学上下文
- 用户显式指定优先级高于当前上下文
- 试卷名称搜索不受教学上下文限制

**5. 资源卡片与动作**
- 资源卡片：标题/标签/元数据行/推荐理由条/操作按钮行
- 支持动作：预览/布置/加入试卷篮/加入备课
- 功能入口卡片：「立即进入」跳转到功能页面

**6. 同步词汇/课文特殊交互**
- 结构化词汇表：课标词/非课标词/语块三区，支持全选/部分选择
- 6 种词汇练习形式：口语跟读/看英选中/单词默写/看中选英/单词听写/听音识词
- 课文层级树：Section→Passage，支持逐句跟读/整篇跟读/整篇背诵
- 生成练习：选中内容 × 练习形式 → 生成作业草稿

**7. 搜索异常处理**
- 搜索无结果：3 层替代推荐（当前单元/同年级/非当前单元）
- 结果过多（>50 条）：分组折叠+筛选收敛，无传统分页
- 完全无法识别：快捷入口引导

**8. 试卷篮**
- Demo 阶段本地存储
- 支持去重、数量角标、面板查看

**9. 技术实现方式**
- 规则引擎匹配（关键词正则匹配+优先级路由）
- 本地 mock 数据（20+ 预设搜索场景）
- Demo 阶段不接入真实大模型

#### v1.0 限制说明

- 不支持口语化表达、简称、近义词、错别字（仅精确关键词匹配）
- 不支持资源名称模糊匹配（仅试卷名称支持省份+年份+考试类型解析）
- 不支持地区词匹配（仅听说场景有简单地区检查）
- 不支持"已识别但当前入口不承接"的精准跳转（如报告/错题本）
- 无法识别时仅展示 3 个快捷入口，无"搜索建议词"或"常用功能推荐"
- 搜索结果仅一个分组维度，无"智能匹配推荐/智能关联推荐"双分组
- 搜索 Loading 为简单状态，无 AI 思考感文案
- 不支持套题意图、微技能意图、选题组卷意图等细分场景
- 部分场景覆盖不完整（如"答题卡"仅匹配制作入口，未关联"我的答题卡"列表）

---

### v1.1（已完成）

- **完成日期**：2026-06-22
- **规则文档**：[`xiaotian-smart-search-v1.1-rules.md`](./xiaotian-smart-search-v1.1-rules.md)
- **评审说明**：[`xiaotian-smart-search-v1.1-review-guide.md`](./xiaotian-smart-search-v1.1-review-guide.md)
- **代码位置**：
  - 意图注册表：`src/ai/search-new/intentRegistry.ts`（35 条意图，单源真）
  - 意图处理器：`src/ai/search-new/intentHandlers.ts`（24 类意图结果构建器）
  - 数据源：`src/ai/search-new/dataSources.ts`（9 组 mock 数据，独立封装）
  - 搜索引擎增强：`src/ai/search-new/searchEnhancer.ts`（双分组/文案/Loading/兜底）
  - 名称匹配：`src/ai/search-new/nameMatcher.ts`
  - 区域匹配：`src/ai/search-new/regionMatcher.ts`
  - UI 组件：`src/ai/components/search-new/`（19 个组件，含 5 个 v1.1 新增）
  - 页面入口：`src/pages/SearchPage.tsx`

#### v1.1 实现能力（共 24 类意图）

**搜索结果框架**：
- 双分组结构：智能匹配推荐 + 智能关联推荐
- AI 理解文案 + AI 思考感 Loading
- 精准跳转：报告/错题/备课/错词 4 种，引导到对应页面
- 无法识别兜底：搜索建议词（9 个）+ 常用功能推荐（6 个）
- 分组内分类 Tab：单类型不展示、多类型独立筛选、点击正确过滤
- 分组标题规范化：去除"精准匹配/推荐资源/替代推荐"前缀
- 语义检测：无意义搜索词强制走兜底

**核心意图（Phase 2, 5 类）**：
- 答题卡：我的答题卡(3条) + 功能入口 + 关联练习入口
- 词表/听写/默写：我的词表 + 听写入口 + 同步单元词汇；听写场景排序优化
- 试卷：我的试卷(5条) + 平台同步/专项/模拟试卷
- 写作：应用文/读后续写 按搜索词选择性展示
- 练习：当前单元练习(基础→综合→检测→阶段)

**扩展意图（Phase 3, 14 类）**：
- 专项/微技能/真题/模拟/套题
- 听力/听力模拟/听说
- 课文/视频/主题视频/配音
- 语法/阅读

**高级能力（Phase 3+）**：
- 关键词归一化：35 条意图统一注册表，含别名/错别字/口语化表达
- 名称匹配：支持完全匹配/前缀匹配/连续包含/关键词拆分/轻度模糊
- 区域精选：14 个省级地区 + 主要城市的区域资源召回
- 优先级排序：精准跳转 > 名称匹配 > 区域精选 > 意图匹配 > 关联推荐 > 兜底

**个人内容卡片**：
- MyContentCard 紧凑卡片组件（两列布局）
- 我的答题卡/词表/试卷统一展示
- 动态主按钮（听写→听默写, 默写→布置默写, 默认→布置默写）
- "查看全部/查看更多"入口

#### v1.1 关键优先级

```
精准跳转 > 名称精确匹配 > 区域精选匹配 > 精确意图匹配 > 子意图匹配 > 泛意图匹配 > 关联推荐 > 兜底
```

| 特殊规则 | 说明 |
|------|------|
| 试卷答题卡 → 答题卡 | 不进入试卷 |
| 单元卷 → 单元练习 | 不进入普通试卷 |
| 听力模拟 → 听力模拟 | 不进普通听力/普通模拟 |
| 听力专项 → 专项 | 不进普通听力 |
| 听说 → 听说 | 不进普通听力 |
| 读后续写 → 写作 | 不进阅读 |
| 词汇薄弱 → 词汇洞察精准跳转 | 不进普通词汇 |
| 报告/错题/备课/错词 → 精准跳转 | 不混入普通资源 |
| 无意义词 → 无法识别兜底 | 不展示普通资源 |

#### v1.1 已知限制

1. 数据多为 mock，后续接真实接口
2. 我的备课路由待确认（`routeConfirmed: false`）
3. 区域精选地区词先覆盖 14 个常见省份，后续根据真实资源扩展
4. 名称匹配为规则匹配，可结合真实搜索日志优化
5. 部分入口（新建答题卡/三方答题卡/查看全部）路由待确认
6. 听写场景下单元词汇动作顺序暂用 v1.0 默认排序，可后续独立调整

#### v1.1 commit 历程

| Hash | 内容 |
|------|------|
| `4f392eb` | Phase 1: 搜索结果框架与基础体验优化 |
| `c7e8841` | Phase 1.5: 统一关键词意图注册表 |
| `0e3f0f7` | Phase 2: 5 类核心意图召回 |
| `028f702` | Phase 3: 14 类扩展资源意图召回 |
| `be3cee9` | ci: GitHub Pages 部署触发 |
| `7dd5b51` | 全量意图规则与回归修复 (185/185 passed) |

---

## 词汇洞察与复习方案 — 版本记录

### v1.0（当前稳定基线）

- **基准日期**：2026-06-16
- **文档**：[`v1.0/04_词汇洞察与复习方案规则口径说明.md`](./v1.0/04_词汇洞察与复习方案规则口径说明.md)
- **代码位置**：
  - 洞察组件：`src/ai/components/vocabulary-insight/`
  - 页面入口：`src/pages/VocabularyInsightPage.tsx`
  - 路由：`/vocabulary-insight`
  - 复习方案弹窗：`ReviewPlanWizard.tsx`
  - 草稿篮面板：`DraftBasketPanel.tsx`

#### v1.0 主要能力

1. **首页词汇洞察卡片**：单张完整宽度卡片，展示洞察结论+数据依据+风险提示
2. **词汇洞察详情页**：诊断总览/高频错词工作台/错误类型洞察/学生洞察/干预记录
3. **复习草稿篮**：跨页面收集词汇，7 天保留期，发布后自动清理
4. **复习方案生成**：5 种复习目标，多天连续复习计划，题型按归因推荐
5. **动态回滚**：后续复习日根据前日错词数据自动生成回滚题
6. **练习报告集成**：复习计划作为聚合卡片混入练习报告列表
7. **数据概览**：覆盖词汇/累计完成率/词汇掌握率提升/回滚题正确率

---

## 版本记录维护规则

1. 每个模块独立版本号，互不影响
2. 当前稳定版本标记为"稳定基线"
3. 新增规则归入下一版本（如 v1.0 → v1.1）
4. 每次版本更新需记录：日期/文档变更/代码变更/新增能力/破坏性变更
5. 破坏性变更需特别标注并说明迁移方案
